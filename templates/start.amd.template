<%= before %>
define('<%= moduleName %>',
[
<%= deps.map(function(dep){return "'" + dep + "'"}).join(',\n') %>
],function (
<%= deps.map(function(dep){return dep.replace(/\//g,'_')}).join(',\n') %>
) {
<%= after %>